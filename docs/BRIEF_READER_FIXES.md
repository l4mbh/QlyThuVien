# 💡 BRIEF: Reader Client Fixes (Critical)

**Ngày tạo:** 2026-04-26
**Trạng thái:** Drafting

---

## 1. VẤN ĐỀ CẦN GIẢI QUYẾT
1.  **Lỗi Unauthorized**: Người dùng Reader không mượn được sách vì hệ thống chưa cấp JWT Token hợp lệ sau khi nhập SĐT.
2.  **Hardcoded Data**: Danh mục (Categories) và nút mượn sách đang được viết cứng trong code frontend, không linh hoạt.
3.  **Thiếu Guest Flow**: Chưa có cơ chế tự động tạo User Guest khi người dùng mới truy cập bằng SĐT lần đầu.

## 2. GIẢI PHÁP ĐỀ XUẤT
1.  **Phone-based Identity**: Chuyển đổi từ lưu SĐT local sang flow `POST /auth/reader-login` để lấy Token.
2.  **Dynamic Categories**: Fetch danh mục từ API `/categories`.
3.  **Smart Action Button**: Nút bấm thay đổi text/logic dựa trên auth và availability (Available/Reserved/Pick up).
4.  **Auto Guest Creation**: Backend tự tạo Reader record (isGuest=true) khi SĐT chưa tồn tại.

## 3. ĐỐI TƯỢNG SỬ DỤNG
- **Reader**: Người dùng vãng lai hoặc khách quen truy cập qua app di động.
- **Admin**: Quản lý danh sách Reader (phân loại Active/Guest).

## 4. TÍNH NĂNG (MVP)
- [ ] Reader Login bằng SĐT (Nhận JWT).
- [ ] Hiển thị danh mục sách từ database.
- [ ] Tự động tạo Reader profile khi login lần đầu.
- [ ] Đồng bộ token cho mọi request (Borrow/Reserve).

---
*Generated by Antigravity*
