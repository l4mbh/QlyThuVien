# Phase 01: Component Design
Status: ⬜ Pending

## Objective
Create the reusable `PageHeader` component with slots for dynamic content and automated metadata fetching.

## Requirements
- Create `src/components/ui/page-header/`.
- Use `useMatches` to fetch the current route's title and description.
- Support `actions` prop for page-specific buttons.
- Implement Glassmorphism styling.

## Implementation Steps
1. [ ] Create directory: `apps/web/src/components/ui/page-header/`
2. [ ] Create file: `apps/web/src/components/ui/page-header/page-header.tsx`
3. [ ] Integrate `MainBreadcrumb` into the new header.
4. [ ] Export component.

## Files to Create/Modify
- `apps/web/src/components/ui/page-header/page-header.tsx` [NEW]
